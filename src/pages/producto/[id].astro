---
import Layout from "../../layouts/Layout.astro";
import Motorcycle from "../../components/Motorcycle.astro";
import productData from "../../data/data.json";

const { id } = Astro.params;

export async function getStaticPaths() {
  // const response = await fetch(
  //   "https://bluemotorsec.com/wp-json/wp/v2/productos-tienda"
  // );
  // const media = await response.json();

  // const paths = media.map((item) => ({
  //   params: {
  //     id: item.id.toString(),
  //   },
  // }));

  // return {
  //   paths,
  //   fallback: false,
  // };

  return [
    { params: {id: "123"} },
  ]
}
const producto = productData.detallesMotocicleta;
// const response = await fetch(
//   `https://bluemotorsec.com/wp-json/wp/v2/productos-tienda/${id}`
// );
// const media = await response.json();
---

<Layout title={`BlueMotors - ${producto.modelo}`}>
  <main class="mx-auto max-w-6xl lg:py-20 py-10 sm:px-6 lg:px-8">
    <Motorcycle
      name={producto.modelo}
      description={producto.descripcion}
      imageUrl={producto.fotos[0].image1}
      price={10000}
      category="Motocicleta"
      mileage="100,000 km"
      engine={producto.rendimiento[0].motor}
      year="2022"
      color={producto.tipoModelo}
      vin={producto.numeroVin}
    />
  </main>
</Layout>; ---
